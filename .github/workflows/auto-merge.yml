name: Auto merge for GH Actions
on: pull_request

permissions:
  contents: write
  pull-requests: write

jobs:
  dependabot:
    runs-on: ubuntu-latest
    if: ${{ github.actor == 'dependabot[bot]' && contains( github.event.pull_request.labels.*.name, 'auto-merge') }}
    steps:
      - name: Enable auto-merge for Dependabot GitHub Actions PRs
        run: gh pr merge --auto --rebase --squash "$PR_TITLE"
        env:
          PR_TITLE: ${{github.event.pull_request.title}}
          GITHUB_TOKEN: ${{secrets.GITHUB_TOKEN}}
